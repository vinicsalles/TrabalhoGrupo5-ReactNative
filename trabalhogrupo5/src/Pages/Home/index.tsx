import React from "react";
import { TextInput } from "react-native";
import { View } from "react-native";

export const Home = () => {
  return (
    <View>
      <TextInput>Bem vindo a Home</TextInput>
    </View>
  );
};
