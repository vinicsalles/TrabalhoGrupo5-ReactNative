import { StyleSheet } from "react-native";

export const styled = StyleSheet.create({
  gradiente: {
    width: "120%",
    height: 96,
    position: "absolute",
    marginLeft: -130,
    marginTop: -50,
    opacity: 0.8,
  },
});
