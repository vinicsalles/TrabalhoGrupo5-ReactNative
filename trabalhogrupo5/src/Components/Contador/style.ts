import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  container: {
    
  },
  timer: {
    width: 140,
    textAlign: 'right',
    fontSize: 30,
    color: "white",
    fontWeight: 'bold',
  },
});
