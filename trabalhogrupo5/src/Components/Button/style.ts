import { StyleSheet } from "react-native";

export const styled = StyleSheet.create({
  button: {
    marginTop: 20,
    width: '50%',
    alignItems: "center",
    alignContent: "center",
    justifyContent: "center",
    borderRadius: 20,
    marginBottom: 100,
    height: 50,
  },
  title: {
    fontSize: 20,
    color: "white",
  },
});
