import { StyleSheet } from "react-native";

export const styled = StyleSheet.create({
  styleInput: {
    marginBottom: -10,
    width: '80%',
    height: 50,
    color: '#000',
    paddingLeft: 20,
    borderRadius: 40,
    fontSize: 15,
  },
})